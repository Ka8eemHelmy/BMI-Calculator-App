import 'package:flutter/material.dart';

class ResultBMIScreen extends StatelessWidget {
  const ResultBMIScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
